class EarnListModel {
  final String? id;
  final String? title;
  final String? bonus;
  final String? image;
  final String? path;

  EarnListModel({this.id, this.title, this.bonus, this.image, this.path});
}
